#!/bin/sh
set -eu

base=$(git merge-base HEAD origin/main)
files=$(mktemp)
trap 'rm -f "$files"' EXIT

git diff --name-only --diff-filter=ACMR -z "$base"...HEAD > "$files"
if [ -s "$files" ]; then
  xargs -0 vp check < "$files"
fi

vp build
